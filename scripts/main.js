// This file orchestrates the initialization of the application

document.addEventListener("DOMContentLoaded", () => {
  // Initialize settings and attach event listeners
  initializeSettingsAndTheme();
  // Render the CV content
  renderCV();
});
